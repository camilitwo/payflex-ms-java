{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd merchant-service && ../mvnw -DskipTests package"
  },
  "deploy": {
    "startCommand": "java -Dserver.port=$PORT -jar merchant-service/target/merchant-service-1.0.0.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/actuator/health",
    "healthcheckTimeout": 300
  }
}

