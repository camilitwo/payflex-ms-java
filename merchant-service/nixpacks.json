{
  "build": {
    "cmd": "cd .. && mvn clean package -pl merchant-service -am -DskipTests && cp merchant-service/target/*.jar merchant-service/app.jar"
  },
  "start": {
    "cmd": "java -Xmx512m -Xms256m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -jar app.jar"
  }
}

