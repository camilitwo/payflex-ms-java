{
  "phases": {
    "setup": {
      "nixPkgs": ["java", "maven"]
    },
    "build": {
      "cmds": [
        "cd .. && mvn clean package -pl merchant-service -am -DskipTests",
        "cp ../merchant-service/target/merchant-service-*.jar app.jar"
      ]
    },
    "start": {
      "cmd": "java -Dserver.port=$PORT -Xmx512m -Xms256m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -jar app.jar"
    }
  }
}
