{
  "build": {
    "cmd": "cd .. && mvn clean package -pl api-gateway -am -DskipTests && cp api-gateway/target/*.jar api-gateway/app.jar"
  },
  "start": {
    "cmd": "java -Xmx512m -Xms256m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -jar app.jar"
  }
}

